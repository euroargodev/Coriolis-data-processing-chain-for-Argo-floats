Liste WMO des flotteurs à déclarer

Récupérer les informations en base via un export csv

Renseigner le fichier _provor_floats_information_co_rt.xls
Collecter les infos nécessaires à partir de l'export csv de la base (ou des meta.nc diffusés) avec get_meta_data_from_data_base.m (ou get_meta_data_prv)

Utiliser generate_json_float_meta pour générer les fichiers JSON de meta-données de ces flotteurs.


ARGOS
mettre tous les fichiers argos à reformater dans le même répertoires
split_argos_cycles => découpe des données Argos (un fichier par flotteur et par passage satellite)
delete_double_argos_split => suppression des fichiers en double
create_argos_cycle_files => creation des fichiers cycles (découpe à g_decPrv_minNonTransDurForNewCycle = 18 heures)
move_and_rename_argos_files => identification des fichiers cycles (nécessite les meta-data JSON)
check_argos_cycle_files => contrôle du résultat obtenu (calage des cycles, cycles manquants et durées des cycles)

effectuer un décodage csv pour vérifier la pertinence du décodage

effectuer un décodage nc

si le flotteur a effectué quelques cycles, vérifier la cohérence des informations temporelles (avec nc_trace_times), au besoin, 'caler' le flotteur


IRIDIUM

pas de préparation des fichiers

effectuer un décodage csv pour vérifier la pertinence du décodage

effectuer un décodage nc

calage  meta + position lâcher sur visualisation des nc


voir si SBD du prelude ne peut pas être récupéré pour la configuration au lâcher
